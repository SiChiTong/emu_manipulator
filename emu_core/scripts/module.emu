print ('Hello There!')
moveTo(self.preconfig_pose['bin_snap'],15)
time.sleep(16)
vision.snapBinImg()
binMsg = self.vision.getBin()
bin_pose_publisher.publish(binMsg)
print ('Moving to home')
moveTo(self.preconfig_pose['home'], 15)
time.sleep(16)
print ('Done!')